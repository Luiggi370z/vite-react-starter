// import { useMutation } from 'react-query';

// import { authenticationApi } from '@/services/api';
// import { IGenericError } from '@/models';

// Use a react query mutation to invoke the login process from api service

const useLogin = () => {
  // return useMutation<unknown, IGenericError, unknown, unknown>('login', (credentials) =>
  //   authenticationApi.login(credentials),
  // );
};

export default useLogin;
