module.exports = {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      // Extend theme colors to use as a class in the app
      colors: {
        primary: '#2A55FF',
      },
    },
  },
  plugins: [],
};
